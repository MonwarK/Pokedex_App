import React from 'react'
import { View, Text } from 'react-native'
import { SafeArea } from '../../../components/SafeArea'

const BaseStatsScreen = () => {
  return (
    <SafeArea>
      <Text>Base Stats Screen</Text>
    </SafeArea>
  )
}

export default BaseStatsScreen
